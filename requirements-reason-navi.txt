# Canonical two-stage ReasonNavi Python dependencies.
#
# This is an intentionally unpinned environment manifest, not a reproducible
# lock file. Install a CUDA-compatible Torch build for the target machine.
# Habitat-Sim 0.3.3 (and its Magnum bindings) must be installed separately,
# normally from the AI Habitat/conda channels; it is not a regular pip
# dependency in this file.

byteplussdkarkruntime
Flask
numba
numpy
opencv-python
Pillow
requests
scikit-learn
scipy
torch
torchvision
